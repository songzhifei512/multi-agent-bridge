# shared-context桥接 worker环境变量模板（公网分发版）。
#拷贝为 ~/.agents/.env或按各 CLI的 config实际注入。
#所有<占位>均为示例，替换为你实际的端点/模型名。
#
#详细说明：见 public-install/ENV_SETUP.md
#配置示例：见 public-install/agents-config-example/.env.example
#
#生成式 agent（必填：Anthropic用于主控；可选：OpenAI/Qwen/DSH用于 worker）：

# ---- Anthropic (Claude Code) -主控/队长（必填） ----
#官方云：https://api.anthropic.com
#获取密钥：https://console.anthropic.com
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_AUTH_TOKEN=<YOUR_ANTHROPIC_API_KEY>

#模型名配置（按实际可用模型填写）：
ANTHROPIC_MODEL=claude-sonnet-4-20250514
ANTHROPIC_DEFAULT_OPUS_MODEL=claude-opus-4-20250514
ANTHROPIC_DEFAULT_SONNET_MODEL=claude-sonnet-4-20250514
ANTHROPIC_DEFAULT_HAIKU_MODEL=claude-haiku-4-20250514

#主控 agent（决定哪个 CLI作队长）：
BRIDGE_CONTROLLER=claude

# ---- OpenAI (Codex CLI) -执行/批量代码生成（可选） ----
#官方云：https://api.openai.com/v1
#获取密钥：https://platform.openai.com
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_KEY=<YOUR_OPENAI_API_KEY>

# ---- Qwen (通义千问) -写文档/PPT +图像分析 VL（可选） ----
#阿里云百炼：https://dashscope.aliyuncs.com/compatible-mode/v1
#获取密钥：https://bailian.console.aliyun.com
# QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# QWEN_API_KEY=<YOUR_QWEN_API_KEY>

# ---- DSH (DeepSeek) -模型中立多后端执行者（可选） ----
#官方云：https://api.deepseek.com
#获取密钥：https://platform.deepseek.com
# DSH_BASE_URL=https://api.deepseek.com
# DSH_API_KEY=<YOUR_DEEPSEEK_API_KEY>

# ---- opencode -限流备路/轻量并发（可选） ----
#复用 OPENAI_BASE_URL和 OPENAI_API_KEY（OpenAI兼容端点）
#安装：npm install -g opencode-ai
#优化启动：export OPENCODE_DISABLE_MODELS_FETCH=1

# ---- Linux向量层 glibc兼容（多数发行版不用；仅特定 glibc环境需） ----
# LD_PRELOAD=<LIBRT_PATH>

# ----状态/工作目录（默认 ~/.agents/{state,work}；可改，勿必要） ----
# BRIDGE_STATE_DIR=
# BRIDGE_WORK_ROOT=
# BRIDGE_WEB_PORT=3000
